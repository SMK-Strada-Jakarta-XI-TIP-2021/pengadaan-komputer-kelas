// Jumlah Komputer
function jumlahKomputer (kolom, baris) {
    var baris, kolom, total;
    baris, kolom  
    total = baris * kolom;
    
    return total;
    }
    
    // Total Jumlah Komputer
    
    console.log ("Komputer kelas 1 = " + jumlahKomputer(8, 10));
    console.log ("Komputer kelas 2 = " + jumlahKomputer(4, 3));
    console.log ("Komputer kelas 3 = " + jumlahKomputer(15, 12));
    console.log ("Komputer kelas 4 = " + jumlahKomputer(6, 8));

    console.log("")

    // Jumlah Harga Komputer

    function hargaKomputer (harga , harga) {
    var harga, harga, total;
        harga, harga
        total = harga * harga;
    
        return total;
    }

